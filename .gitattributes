# Windows-based repo (PowerShell/Windows Hello/PowerToys - see README):
# CRLF is the working-tree norm, declared here per-repo (never core.autocrlf).
* text=auto eol=crlf

# Git hooks run under Git-for-Windows sh, which requires LF.
.githooks/* text eol=lf
